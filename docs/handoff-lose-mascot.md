# Задача: lose-маскот (без коммита)

Делай сразу. Не пиши @hermes через `message_agent` — его чат занят десктопом (`target_busy`). Результат только файлами.

## Что сломано

`src/client/app/ui/result/mascot_lose_00.png` … `_05.png`

- 00 = копия idle
- 03–04 магента
- 05 обрезан; на нём анимация зависает

## Спека

- Тот же рыцарь, что idle/win (шлем, шипы, круглый щит, серая броня, молот)
- Ровно 6 кадров, 128×128 RGBA, прозрачный фон, без #FF00FF
- Оседание/посадка, не взрыв
  - 00 уже сдувается
  - 01–02 почти на колени
  - 03 садится
  - 04 голова ниже, молот у ног
  - 05 холд: целая сидящая фигура по центру
- Код не трогать. Коммит не делать.

PixelLab: `PIXELAB_API_TOKEN` в `.env` профиля, скилл `pixellab-sprites`.

## Сдача

1. Перезапиши PNG по тем же путям.
2. Допиши `docs/handoff-lose-mascot.status.md`: готово / что осталось / пути.
3. Пользователь смотрит http://localhost:5173/ → «сдаться». Сдачу читает @producer с диска. Код не трогать, пока кадры ок.
