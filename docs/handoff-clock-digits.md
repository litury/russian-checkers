# Цифры: на 1px спрайта вниз

Человек: 20px чуть заезжает на верх колодца, снизу пусто. Опустить на **1 native px**.

Колодец на `hud_clock_e_*.png` 64×40: чёрный экран примерно **y=20…30**, x≈26…48. Сейчас `hudClockEWellNativeY = 23` (ближе к верху). Поставить **24**.

`hudClockEWellX` не трогать. Шрифт 20, имена 15, лампы как есть. За безель всё ещё не выходить.

Коммит/пуш — только если скажет человек.
